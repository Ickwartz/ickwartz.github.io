- AM ENDE: Bootstrap local nutzen

- check vor insert ob Eintrag schon vorhanden


- Funktionen
    - Übungen erstellen, editieren und löschen(als veraltet markieren für "legacy content")
        --> löschen nur wenn keine Verbindung zu Trainingsplan
    - Trainingsplan erstellen
    - Trainingsplan mit User verknüpfen
    - Kalender für Trainingspläne 
        --> Übungen für dieses Datum oder Meldung das keine Übungen für diesen Tag vorhanden
    - Registrieren nur wenn auf vorgefertigter Liste


- Account System
    - in login_route statt Email in Session Var Vorname
    - Display des Usernamens auf Website schöner machen 
    - lose ends bei pw mismatch etc --> redirects
    - nur für admin sichtbare pages auch nur als Admin anzeigbar machen

- DB Handling
    - umstellung placeholders to named placeholders

- READ
    - decorators
        --> maybe für generic table_functions

- Tests
    - für alle Table Klassen Read und Write

- Fragen
    - Sollen Übungen wenn sie aktualisiert werden auch in alten Trainingsplänen angepasst werden?
        --> History?
            --> Nope
            

- Refactor
    - Frontend und Backend in separate Folder

- Trainingsplan:
    - Tabelle:   training_id | datum | user
    - personalized_exercises bekommen training_id
    - wenn man Plan für User darstellt:
    --> SELECT * FROM training WHERE datum = $datum AND user = $user;
    --> id 
    --> Anzeige: SELECT * FROM pe WHERE training_id = $training_id;
        --> coole join magic machen


Path Var für VS CODE Shell?


Snackbar Message double

Trainingsplan drucken